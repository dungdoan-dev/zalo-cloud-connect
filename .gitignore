.env
node_modules/
coverage/
*.log

# Secrets and deployment credentials
deploy/key/
*.pem
*.key
.github/*.local

# Runtime data written on the server. These files can contain SIP credentials,
# Meta app secrets or user webhook payloads and must never be committed.
data/whatsapp-calling.json
data/webhooks.ndjson
data/freeswitch/whatsapp-vars.xml
data/freeswitch/whatsapp-dialplan.xml
